<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <!-- Permiso para recibir SMS -->
    <uses-permission
        android:name="android.permission.RECEIVE_SMS" />
    <uses-permission
        android:name="android.permission.RECEIVE_EMAIL" />
    <!-- Permiso para enviar SMS -->
    <uses-permission
        android:name="android.permission.SEND_SMS" />

    <!-- Permiso para acceso a Internet (necesario para enviar correos electrónicos y notificaciones push) -->
    <uses-permission
        android:name="android.permission.INTERNET" />

    <!-- Permiso para recibir notificaciones push (dependiendo de la implementación, puede variar) -->
    <uses-permission
        android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- El siguiente permiso es específico para Firebase Cloud Messaging -->
    <uses-permission
        android:name="your_package_name.permission.C2D_MESSAGE" />

    <uses-permission
        android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission
        android:name="android.permission.READ_SMS" />
    <uses-permission
        android:name="android.permission.READ_PHONE_NUMBERS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/back_button"
        android:supportsRtl="true"
        android:theme="@style/Theme.EFINDER"
        tools:targetApi="31">
        <activity
            android:name=".AgregarPagoActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrarActivity"
            android:exported="false" />
        <activity
            android:name=".TemaActivity"
            android:theme="@style/AppTheme"
            android:exported="false"
            />

        <activity
            android:name=".LoginActivity"
            android:exported="true" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity
            android:name=".NotificacionesActivity"
            android:exported="false" />
        <activity
            android:name=".PagoActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"/>

            <!-- ... otras configuraciones ... -->

            <!-- Declara el receptor de SMS -->

        <receiver android:name=".SMSReceiver" android:enabled="true" android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

            <!-- ... otras configuraciones ... -->


    </application>

</manifest>